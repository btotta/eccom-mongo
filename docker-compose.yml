version: "3.9"
services:
  app:
    build:
      context: .
    container_name: eccom-api
    restart: unless-stopped
    expose:
      - "8080"
    networks:
      - nginx_default

networks:
  nginx_default:
    external: true